import { styled } from "@mui/material/styles";

export const LabelsContainer = styled("div")({
    display: "flex",
    flexFlow: "row wrap",
    gap: "1em",
    padding: "1em",
    borderRadius: "8px",
    width: "100%",
    alignItems: "center",
    justifyContent: "center",
    
});