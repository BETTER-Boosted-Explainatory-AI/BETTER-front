import { createTheme } from "@mui/material/styles";

const BetterTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
          main: '#222831',
        },
        secondary: {
          main: '#76abae',
        },
      },
});

export default BetterTheme;